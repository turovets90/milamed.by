<!DOCTYPE html>
html
    head
        - var page = { title: '404 - станица не найдена' }
        include inc/head.pug
    body#body
        include inc/header.pug
        main.page
            section.title_crumb_section.pb-3
                .container
                    ul.breadcrumb
                        li.breadcrumb-item
                            a(href="#") Главная
                        li.breadcrumb-item
                            a(href="#content") #{page.title}
                    h1 #{page.title}
            section
                .container
                    p Неправильно набран адрес, или такой страницы на сайте больше не существует.
                    p Вернуться на <a href="/">главную
        include inc/footer.pug
        include inc/scripts.pug